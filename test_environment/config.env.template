# =============================================================================
# Test Environment Configuration
# =============================================================================
# Copy this file to config.env and fill in your values:
#   cp config.env.template config.env
# =============================================================================

# GCP Configuration
PROJECT_ID=your-gcp-project-id
REGION=us-central1

# Provider identity (used as JWT audience for DCR)
PROVIDER_URL=https://test-agent-provider.example.com

# Mock IdP API Token (used by both mock-idp and marketplace-handler)
MOCK_API_TOKEN=mock-api-token-for-testing

# Pub/Sub Configuration
PUBSUB_TOPIC_NAME=test-marketplace-orders
PUBSUB_SUBSCRIPTION_NAME=test-marketplace-orders-sub

# Provider ID (used in mock procurement API paths)
PROVIDER_ID=test-provider
